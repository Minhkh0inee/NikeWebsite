
            <div class="mb-5">
                <img class="container" width="100%" height="250" src="/img/Screenshot 2022-12-07 222309.png"/>
            </div>  

<div class="mt-4">
    <div class="row">
        {{#each courses}} <!-- loop for each element in db -->
        <div class="col-sm-6 col-lg-4 mb-5">
            <a href="/courses/{{this.slug}}"> <!-- gan slug cho url-->
                <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
            </a>
            <div class="card card-course-item">
            
        <div class="card-body" style="cursor:pointer">
            <a href="/courses/{{this.slug}}"></a>
                <h5 class="card-title">{{this.name}}</h5>
            </a>    
            <p class="card-text">{{this.description}}</p>
            <h6 class="card-text">{{this.price}}</h6>
        </div>
    </div>
        </div>

    {{/each}}
    </div>
</div>
